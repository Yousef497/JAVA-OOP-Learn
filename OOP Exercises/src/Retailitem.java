// working with interfaces
// interface is like abstract class, but better as it determines the methods that must
// be implemented in the classes
// so it is just like a contract that contains the requirements that must be implemented

public interface Retailitem {
    public double getItemPrice();
}
